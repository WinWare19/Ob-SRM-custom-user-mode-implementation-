# Ob-SRM-custom-user-mode-implementation-
a user mode implementation of two major kernel executive managers : the Object Manager which is responsible for objects create/deletion and lifecycle tracking, and the Security Reference Monitor which is responsible for access checking to determine if a user has the right to do such an operation on an object or on the system.
